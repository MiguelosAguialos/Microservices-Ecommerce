spring:
  application:
    name: configserver
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file:./configs,file:../configs,classpath:/configs
server:
  port: 8888
